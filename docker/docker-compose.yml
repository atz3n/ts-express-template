version: "3.8"
networks: 
  crud-greeter-network:
    external: false
services:
  crud-greeter:
    image: crud-greeter
    networks: 
      - crud-greeter-network
    ports:
      # extern:intern
      - 3000:3000
    environment:
      - AUTH_TOKEN=your-super-secret-token
      - PORT=3000